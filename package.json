{
  "name": "backend",
  "version": "1.0.0",
  "main": "api/index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "vercel-build": "echo \"Skipping build for Vercel\"",
    "build": "echo \"Build complete\""
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": ["code-review", "ai", "gemini", "express"],
  "author": "",
  "license": "ISC",
  "description": "Backend API for AI-powered code review application",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
